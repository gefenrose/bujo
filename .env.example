# Google Calendar sync — OAuth Client ID from Google Cloud Console
# (Console → APIs & Services → Credentials → OAuth client ID → Web application).
# Not secret: this is a public client identifier, safe to embed in client-side code.
VITE_GOOGLE_CLIENT_ID=
